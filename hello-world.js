ReactDOM.render(
  //atualiza os elementos DOM (nossa tela)
  React.createElement("h1", "null", "Hello World"), // Criando um elemento para ser injetado
  document.getElementById("root") // Injetando o elemento no root
);
